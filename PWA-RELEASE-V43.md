# LGS 2027 Arena PWA V4.3

Production PWA release marker.

- Android 16 / Chrome direct-entry gate waits for the app core to be ready.
- Offline cache matches legacy query-string asset URLs with `ignoreSearch`.
- Critical app-shell files are installed atomically before the Service Worker activates.
- Student `localStorage` progress is preserved.
- GitHub Pages production deployment runs only from `main`.

Live project path: `https://zorbirey.github.io/LGS2027-Arena/`
